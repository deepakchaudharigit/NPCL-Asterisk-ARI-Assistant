[pytest]
# Pytest configuration for Gemini Voice Assistant tests

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    unit: Unit tests for individual components
    integration: Integration tests for component interactions
    performance: Performance and latency tests
    audio: Audio processing and quality tests
    websocket: WebSocket communication tests
    e2e: End-to-end workflow tests
    slow: Tests that take longer to run

# Async support
asyncio_mode = auto

# Output options
addopts = 
    -v
    --tb=short
    --disable-warnings
filterwarnings =
    ignore::DeprecationWarning:speech_recognition.*
    ignore:aifc was removed in Python 3.13.*:DeprecationWarning
    ignore:There is no current event loop:DeprecationWarning

# Minimum version
minversion = 6.0

# Test timeout (in seconds)
timeout = 300

# Coverage options (if pytest-cov is installed)
# addopts = --cov=src --cov-report=html --cov-report=term-missing

# Parallel execution (if pytest-xdist is installed)
# addopts = -n auto